---
- hosts: webserver1
  vars_files:
    - config.yml
  tasks:
    - name: include node
      include_role:
        name: nodejs
      vars:
        node_version: "{{node_version_webserver1}}"



- hosts: webserver2
  vars_files:
    - config.yml
  tasks:
  - name: include node
    become: yes
    include_role:
      name: nodejs
    vars:
      node_version: "{{node_version_webserver2}}"
